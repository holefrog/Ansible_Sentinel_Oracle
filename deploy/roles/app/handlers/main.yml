---
# agent/handlers/main.yml
# v1

- name: Reload systemd
  ansible.builtin.systemd:
    daemon_reload: true

- name: Restart bot
  ansible.builtin.systemd:
    name: "{{ service_name }}"
    state: restarted

