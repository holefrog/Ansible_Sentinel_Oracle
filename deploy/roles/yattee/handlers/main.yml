---
- name: Reload Nginx
  ansible.builtin.systemd:
    name: nginx
    state: reloaded

- name: Restart yattee-server
  ansible.builtin.command:
    cmd: docker compose up -d --build
    chdir: "{{ yattee_install_dir }}"
  changed_when: false
